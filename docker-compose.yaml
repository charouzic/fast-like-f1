version: '3.4'

services: 
  #postgres db for local development
  basketprocessor.postgres-database:
    image: postgres:14.1
    environment:
      POSTGRES_USER: "api"
      POSTGRES_PASSWORD: "api"
    ports:
        - "5432:5432"